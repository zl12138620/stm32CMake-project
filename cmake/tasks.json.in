{
    // 本文件由 CMake 配置阶段自动生成（模板：cmake/tasks.json.in），请勿手动修改。
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --preset @CMAKE_PRESET_CONFIG_NAME@; if ($LASTEXITCODE -eq 0) { cmake --build --preset @CMAKE_PRESET_BUILD_NAME@ }",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
